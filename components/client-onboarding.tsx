'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { Icon } from './icons';
export function ClientOnboarding() { const router = useRouter(); const [form,setForm] = useState({business_name:'',offer:'',target_customer:'',average_deal_value:1000,tone:'professionnel et direct'}); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/onboarding',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const p=await r.json();setBusy(false);if(!r.ok)return setError(p.error);router.push('/client');} return <form onSubmit={submit} className="constellation-panel mx-auto max-w-3xl"><p className="eyebrow">Configuration guidée</p><h2 className="mt-2 text-2xl font-semibold">Apprends ton activité à JARVIS</h2><p className="mt-2 text-sm text-slate-500">Ces informations déterminent qui cibler et comment présenter ton offre.</p><div className="mt-7 grid gap-5"><label className="text-xs text-slate-500">Nom de l’activité<input className="field mt-2" value={form.business_name} onChange={e=>setForm({...form,business_name:e.target.value})} required/></label><label className="text-xs text-slate-500">Ce que tu vends<textarea className="field mt-2 min-h-28" value={form.offer} onChange={e=>setForm({...form,offer:e.target.value})} placeholder="Résultat livré, délai, différence…" required/></label><label className="text-xs text-slate-500">Ton client idéal<textarea className="field mt-2 min-h-28" value={form.target_customer} onChange={e=>setForm({...form,target_customer:e.target.value})} required/></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-xs text-slate-500">Valeur moyenne d’une vente (€)<input type="number" className="field mt-2" value={form.average_deal_value} onChange={e=>setForm({...form,average_deal_value:Number(e.target.value)})}/></label><label className="text-xs text-slate-500">Ton de communication<select className="field mt-2" value={form.tone} onChange={e=>setForm({...form,tone:e.target.value})}><option>professionnel et direct</option><option>chaleureux et accessible</option><option>premium et concis</option></select></label></div></div><button className="btn-primary mt-7 w-full" disabled={busy}>{busy?'Configuration…':<>Activer mon espace <Icon name="arrow"/></>}</button>{error&&<p className="mt-4 text-sm text-red-300">{error}</p>}</form>; }
