import { Shell } from '@/components/shell'; import { missions } from '@/lib/data'; import { Progress } from '@/components/ui';
export default function MissionsPage(){return <Shell title="Missions"><div className="card overflow-hidden p-0"><div className="border-b p-5"><input className="w-full bg-transparent text-sm outline-none placeholder:text-slate-500" placeholder="Search missions…"/></div><div className="divide-y">{missions.map(m=><div key={m.id} className="p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-medium">{m.title}</h2><p className="mt-1 text-sm text-slate-500">Assigned to {m.agent} · Updated {m.updated}</p></div><span className="rounded-full bg-brand/15 px-3 py-1 text-xs text-violet-300">{m.status}</span></div><div className="mt-4 max-w-xl"><Progress value={m.progress}/></div></div>)}</div></div></Shell>}
