import { Shell } from '@/components/shell';
const settings=[['Supabase','Persistence, authentication and agent memory','Not connected'],['Cloudflare Workers','Mission execution and tool gateway','Not connected'],['LLM provider','Select OpenAI, Anthropic, Gemini or Ollama','Not configured']];
export default function SettingsPage(){return <Shell title="Settings"><p className="mb-6 text-slate-400">Connect Forge AI to the services that run your team.</p><div className="space-y-4">{settings.map(([name,copy,status])=><section key={name} className="card flex flex-wrap items-center justify-between gap-4"><div><h2 className="font-semibold">{name}</h2><p className="mt-1 text-sm text-slate-400">{copy}</p></div><div className="flex items-center gap-4"><span className="text-xs text-amber-300">{status}</span><button className="btn-muted">Configure</button></div></section>)}</div></Shell>}
